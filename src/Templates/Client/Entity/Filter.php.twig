<?php

namespace {{ api.namespace }}\Entity;

{% if type.extendsBaseFilter %}
use Paysera\Component\RestClientCommon\Entity\Filter;

class {{ type.name }} extends Filter
{% else %}
class {{ type.name }}
{% endif %}
{
{% for property in type.properties %}
{% include 'Property/getter.php.twig' with { 'property': property } only %}

{% include 'Property/setter.php.twig' with { 'property': property } only %}

{% endfor %}
}
