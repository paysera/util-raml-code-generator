<?php

namespace {{ api.namespace }}\Entity;

use Paysera\Component\RestClientCommon\Entity\Entity;

class {{ type.name }} extends Entity
{
{% for property in type.properties %}
    {%- include '@PayseraPhpGenerator/RestClient/Entity/Property/getter.php.twig' with { 'property': property } only -%}
    {%- include '@PayseraPhpGenerator/RestClient/Entity/Property/setter.php.twig' with { 'property': property } only -%}
{% endfor %}
}
