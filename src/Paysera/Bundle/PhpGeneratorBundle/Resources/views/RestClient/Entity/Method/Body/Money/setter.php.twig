$this->set('{{ property.name }}_amount', ${{ property.name|to_variable_name }}->getAmount());
        $this->set('{{ property.name }}_currency', ${{ property.name|to_variable_name }}->getCurrency());
