<?php

namespace {{ api.namespace }};

final class {{ entity_name|to_class_name }}Permissions
{
    {% for sub_resource in flatten_sub_resources(resource) -%}
    {% for method in sub_resource.methods -%}
    const {{ generate_method_name(method, sub_resource, api)|to_snake_case|upper }} = '{{ generate_method_name(method, sub_resource, api)|to_snake_case }}';
    {% endfor -%}
    {% endfor %}

}
