{% spaceless %}
    {% if property.type == constant('TYPE_REFERENCE', property) -%}
        {{ property.reference|extract_type_name -}}
    {% endif %} ${{ property.name|to_variable_name }}
{% endspaceless %}
