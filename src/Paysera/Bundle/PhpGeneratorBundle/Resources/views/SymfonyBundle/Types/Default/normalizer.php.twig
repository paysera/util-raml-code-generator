{% if property.type == constant('TYPE_REFERENCE', property) %}
'{{ property.name|to_snake_case }}' => $entity->{{ php_generate_getter_name(property) }}() !== null ? $this->{{ property.reference|extract_type_name|to_variable_name }}Normalizer->mapFromEntity($entity->{{ php_generate_getter_name(property) }}()) : null,
{% else %}
'{{ property.name|to_snake_case }}' => $entity->{{ php_generate_getter_name(property) }}(),
{% endif %}
