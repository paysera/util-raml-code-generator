
{% if property.required == false %}
        if (this.get('{{ property.name }}')['amount'] === null || this.get('{{  property.name }}')['currency'] === null) {
            return null;
        }
{% endif %}
        return new Money(this.get('{{ property.name }}')['amount'], this.get('{{ property.name }}')['currency']);
