        const {{ generate_method_name(method, resource) }}Original = client.{{ generate_method_name(method, resource) }}.bind(client);
        client.{{ generate_method_name(method, resource) }} = (...args) => {
            return this.$q.when({{ generate_method_name(method, resource) }}Original(...args));
        };
