import { Result } from 'paysera-http-client-common';

import {{ type.itemsType }} from './{{ type.itemsType }}';

class {{ type.name }} extends Result {

    /**
     * @param {Array} data
     * @returns {{ ('{' ~ type.itemsType ~ '}')|raw }}
     */
    createItem(data) {
        return new {{ type.itemsType }}(data);
    }
}

export default {{ type.name }};
